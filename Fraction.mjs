use 'strict';

import * from './ext_libs/ENUMJS/enumjs.mjs'

const sign = new Type({ 'sign': true })

export default class Fraction(){
    constructor(){
        this.n = numerator;
        this.d = denominator;
        this.sign = new Enum( sign )
    }

    setND( numerator, denominator){
        
    }

    #verify(){}
}



